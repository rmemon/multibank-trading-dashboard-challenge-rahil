PORT=3001
CORS_ORIGIN=http://localhost:5173

# JWT signing (use a long random string in production)
JWT_SECRET=change-me-use-openssl-rand-base64-32
# Shorter sessions recommended for trading-style apps (e.g. 15m, 30m, 1h)
JWT_EXPIRES_IN=1h
