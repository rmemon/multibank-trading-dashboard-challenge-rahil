/**
 * Placeholder layout — feature work (API, charts, WebSocket) comes in later steps.
 */
export function DashboardShell() {
  return (
    <main className="dashboard-shell">
      <header className="dashboard-shell__header">
        <h1>Trading dashboard</h1>
        <p className="dashboard-shell__subtitle">Frontend shell — connect APIs and UI.</p>
      </header>
    </main>
  );
}
