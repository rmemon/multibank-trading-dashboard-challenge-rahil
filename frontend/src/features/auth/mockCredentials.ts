/**
 * Mock credentials for development / demo login.
 * Replace with real auth (API + tokens) later.
 */
export const MOCK_CREDENTIALS = {
  email: "demo@trading.local",
  password: "demo1234",
} as const;
